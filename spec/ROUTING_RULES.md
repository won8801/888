# ROUTING_RULES

## 기본
- 항상 QA는 호출한다.
- Front/Back은 키워드로 결정하되, 애매하면 둘 다 호출한다.

## Front 키워드(예)
- 버튼, 클릭, 화면, 페이지, 레이아웃, CSS, UI, 프론트, JS, 이벤트, 모달

## Back 키워드(예)
- API, DB, 쿼리, SQL, 로그, 에러코드, 서버, 톰캣, 응답, 세션, 인증

## 애매/혼합
- 둘 다 호출
